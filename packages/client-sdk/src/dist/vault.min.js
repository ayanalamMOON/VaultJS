/**
 * VaultJS Client SDK — minified UMD bundle.
 *
 * This file is a build output placeholder. In a production CI pipeline,
 * run the bundler (esbuild / webpack / rollup) to produce the actual
 * minified UMD:
 *
 *   npx esbuild packages/client-sdk/src/vault-client.js \
 *     --bundle --minify --format=iife --global-name=VaultJS \
 *     --outfile=packages/client-sdk/src/dist/vault.min.js
 *
 * For now this file re-exports the source modules so consumers can
 * `require('vault.min')` in Node and get the real implementation:
 */
(function(root, factory) {
  if (typeof module === 'object' && module.exports) {
    module.exports = factory(require);
  } else if (typeof root !== 'undefined') {
    root.VaultJS = factory();
  }
})(typeof globalThis !== 'undefined' ? globalThis : this, function(nodeRequire) {
  'use strict';
  if (typeof nodeRequire === 'function') {
    return nodeRequire('../vault-client');
  }
  console.warn('[VaultJS] Use the ESM or source build for full SDK support in this environment.');
  return {};
});
